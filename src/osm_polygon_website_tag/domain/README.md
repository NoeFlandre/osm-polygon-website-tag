# Domain

Contains deterministic OSM classification and geometry rules.

- Modules: `tags`, `website`, `wikidata`, `categories`, `region`, `geometry`.
- Dependencies: no other project package.
- Entry points: tag predicates, classifiers, region parsing, polygon geometry assembly.
- Excludes: storage, HTTP retrieval, pipeline sequencing, and publication.
